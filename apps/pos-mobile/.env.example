# Base URL the mobile app talks to directly (react-native-config, no server-side proxy). Post-
# cutover this is the VPS-hosted API's public hostname, not the old Render URL — see
# apps/api/docs/DEPLOY_NATIVE.md.
API_BASE_URL=