NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
NEXT_PUBLIC_API_PROXY_BASE_URL=http://localhost:3000/api
# apps/order-next's local dev server — run it with `nx run order-next:dev -- --port 3001`
# so it doesn't clash with this app's own port 3000.
NEXT_PUBLIC_ORDER_APP_BASE_URL=http://localhost:3001
