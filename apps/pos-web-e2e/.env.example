E2E_USERNAME=
E2E_PASSWORD=

# transactions.kdsNotifications.spec.ts shells out to the mysql CLI to check
# for a kds_notifications row (docs/prd-kds-order-notifications.md phase 9)
# — there is no REST endpoint for that table by design. Match whatever
# apps/api/.env points at; e2e-main.yml already sets these for CI.
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USERNAME=
DB_PASSWORD=
DB_NAME=