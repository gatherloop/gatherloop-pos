import { Layout } from '../../../base';
import { BudgetList } from '../../widgets';

export const BudgetListScreen = () => {
  return (
    <Layout title="Budgets">
      <BudgetList />
    </Layout>
  );
};
