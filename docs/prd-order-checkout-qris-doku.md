# PRD: Order App Checkout — QRIS Payment via DOKU

**Status:** Draft for review
**Scope:** the second half of `docs/prd-table-ordering.md` — turning a customer's cart into a **paid** `Transaction` through a **QRIS** payment served by **DOKU**, with our own checkout UI.
**Supersedes:** D10 in `docs/prd-table-ordering.md` (the flagged "Pembayaran QRIS — segera hadir" stub) and the `NEXT_PUBLIC_ORDER_CHECKOUT_ENABLED` behaviour it describes.

---

## Problem Statement

`docs/prd-table-ordering.md` shipped the customer half of self-ordering: a guest scans the table QR, browses the menu, and builds a server-side cart. It stops at a stub. `libs/ui/src/presentation/views/screens/order/CheckoutScreen.tsx` renders a message saying QRIS is coming and that **the order has not been sent to the kitchen**, and `libs/ui/src/app/order/Checkout.tsx` creates nothing.

So today the feature does not actually remove the bottleneck it was built for. A guest still has to walk to the counter, re-state an order the system already holds, and pay a cashier. The cart is a nicer way to compose an order, not a way to place one.

This PRD closes the loop: **cart → QRIS payment → paid `Transaction` visible on the POS**, with the payment gateway terminating entirely inside the Go API.

### Goals

1. A guest with a non-empty cart can pay by **QRIS**, in our own UI, without leaving the order app.
2. The QR is generated by **DOKU** through our Go API. The customer bundle never talks to DOKU and never holds a DOKU credential.
3. On success the guest sees a payment-success message and is then taken to a screen telling them their order is being prepared, identified by their name and their table.
4. A `Transaction` exists in the POS for every successful checkout, **priced at the moment of checkout**, marked paid, with the correct wallet credited.
5. Staff can tell at a glance — and filter — which transactions came from the order app and which a cashier keyed in.
6. Every phase ships as one small, independently reviewable PR.

### Non-Goals

| Not doing | Why |
|---|---|
| Notifying barista / kitchen when an order-app transaction appears | Explicitly excluded by the requester. The transaction shows up in the existing POS list and the existing order-slip print; nothing pushes. A kitchen display or push channel is its own project. |
| Notifying the customer when their order is ready | Same. The flow ends at "sedang disiapkan". There is no customer-facing order-status polling beyond payment confirmation. |
| Any payment method other than QRIS | Requested. The gateway port is shaped so a second method is additive, but no second method is built, configured, or rendered. |
| Refunds, voids, partial payments, tips, service charge, tax lines | None exist in the POS today. A wrong order-app payment is resolved the way a wrong cashier payment is: staff `Unpay` (within 24 h) and settle by hand. |
| DOKU Checkout (hosted payment page) | Explicitly excluded — we build our own UI on the Direct API. |
| Coupons / discounts at customer checkout | `TransactionCoupon` stays a staff-only concept. Order-app transactions are created with `transactionCoupons: []`. |
| Splitting the bill, paying for someone else's table, order history | No accounts exist (the session is anonymous), so none of these have an owner. |
| Rentals through the order app | Unchanged from `prd-table-ordering.md`: the customer menu is `saleType=purchase` only. |
| POS-side QRIS (a cashier charging a walk-up guest through DOKU) | The cashier already has a physical QRIS acceptance path and books it against the `QRIS` wallet manually. Bringing that into DOKU is a separate migration with its own reconciliation story. |

---

## Context: The Existing System

### What already exists and will be reused verbatim

| Thing | Where | Why it matters here |
|---|---|---|
| Server-side cart, keyed by anonymous session | `apps/api/domain/cart_{entity,usecase,repository}.go`, `carts` / `cart_items` (migration `000020`) | The checkout input. `Cart.Status` already has a `converted` value that nothing sets yet — this PRD is what finally sets it. |
| `X-Session-Id` capability model | `RequireSessionId` in `apps/api/presentation/restapi/base_middlewares.go`; `SessionRepository` / `CookieSessionRepository` on the frontend | Checkout routes reuse it unchanged. D8 in `prd-table-ordering.md` flagged that this assumption "must be re-evaluated when a session starts owning money-shaped records" — that re-evaluation is D8 below. |
| Server-authoritative cart pricing | `CartUsecase`, D7 in `prd-table-ordering.md` — items store only `variant_id`, `amount`, `note`; price is derived at read time | The client never sends an amount to charge. The charged amount is computed server-side at checkout. |
| `Transaction` with an unpaid → paid lifecycle | `apps/api/domain/transaction_{entity,usecase}.go` | `CreateTransaction` already snapshots `price`, `productName` and option values into `transaction_items`. `PayTransaction` already credits the wallet, subtracts `paymentCostPercentage` (the MDR) and derives `totalIncome` from the recipe's food cost. **This PRD adds no money math.** |
| Wallets with an MDR percentage and a payment-target flag | `apps/api/domain/wallet_entity.go`; `docs/prd-wallet-payment-eligibility.md` | The catalog already contains a `QRIS` wallet with a `paymentCostPercentage`. DOKU settlements land there. |
| `PublicTable` on the cart response | `Cart.table` in `libs/api-contract/src/api.yaml` | The table label is already available at checkout time, so the transaction can carry it without a new lookup. |
| `react-native-qrcode-svg` | root `package.json` dependency | The QR is rendered client-side from DOKU's `qrContent` string. No image round-trip, no new dependency. |
| Order app SSR composition | `docs/trd-order-app-composition-and-ssr.md`; `apps/order-web/src/pages/t/[code]/**` | New routes follow P6: `getServerSideProps` seeds session + table, the composition root builds repositories and usecases, the handler maps machine state to screen props. |
| Clean Architecture on both sides | `README.md` §3; D13 in `prd-table-ordering.md` | **Normative.** A checkout PR that puts an HTTP call in a screen, or DOKU knowledge in the domain layer, should be sent back. |

### What the stub looks like today

```
libs/ui/src/app/order/Checkout.tsx                                   composition root, builds only TableResolveUsecase
libs/ui/src/presentation/handlers/order/CheckoutHandler.tsx          table shell + an `enabled` boolean
libs/ui/src/presentation/views/screens/order/CheckoutScreen.tsx      two EmptyView messages
apps/order-web/src/pages/t/[code]/checkout.tsx                       getServerSideProps → Checkout
apps/order-web/.env.example                                          NEXT_PUBLIC_ORDER_CHECKOUT_ENABLED
```

`CartScreen`'s Checkout button already routes to `/t/{code}/checkout` regardless of the flag — that was deliberate (D10), so this PRD swaps the screen's body and never touches the cart.

### The gaps this PRD must close

1. **No payment domain at all.** No gateway client, no payment records, no webhook route, no reconciliation.
2. **No way to tell a transaction's origin.** `transactions` has no `source` column; the POS list and detail screens have nowhere to show one and no filter to select one.
3. **`Transaction.name` is a customer name typed by a cashier** (the POS list searches on it, placeholder "Search Customer Name"). An anonymous guest supplies none, and nothing in the order app has ever asked for one.
4. **Nothing remembers a guest between orders.** The session ID identifies a device; no table holds anything about the person using it, so a returning guest would retype whatever we ask for.
5. **`Cart.Status = converted` is never set,** so a guest who pays would keep mutating the cart their payment was computed from.
6. **The API has no unauthenticated write route.** Every existing route is either `CheckAuth` (staff) or `RequireSessionId` (the guest's own cart). A gateway webhook is neither — it is an unauthenticated write authorised by a signature.

### What DOKU gives us

DOKU exposes QRIS through the Bank Indonesia **SNAP** standard (`Standar Nasional Open API Pembayaran`), Merchant-Presented Mode dynamic QR. The surface we need is four calls:

| Purpose | Method + path | Notes |
|---|---|---|
| Get access token | `POST /authorization/v1/access-token/b2b` | Headers `X-CLIENT-KEY`, `X-TIMESTAMP`, `X-SIGNATURE`. Asymmetric signature: `SHA256withRSA` over `clientId + "|" + X-TIMESTAMP` with our private key. Response carries `accessToken` and `expiresIn` (900 s). |
| Generate dynamic QRIS | `POST /snap-adapter/b2b/v1.0/qr/qr-mpm-generate` | Returns `responseCode`, `referenceNo`, `partnerReferenceNo`, `qrContent`. |
| Query payment status | `POST /snap-adapter/b2b/v1.0/qr/qr-mpm-query` | Body carries `originalPartnerReferenceNo`, `originalReferenceNo`, `merchantId`, `serviceCode: "47"`. |
| Payment notification (inbound) | DOKU → our URL, registered in the DOKU dashboard | Body carries `originalPartnerReferenceNo`, `originalReferenceNo`, `latestTransactionStatus` (`"00"` = success), `transactionStatusDesc`, `amount`. DOKU treats **any HTTP 200** as delivered. |

Transactional calls (everything but the token) are signed **symmetrically**, HMAC-SHA512 with the Client Secret over:

```
stringToSign = HTTPMethod + ":" + EndpointUrl + ":" + AccessToken + ":"
             + Lowercase(HexEncode(SHA-256(minify(RequestBody)))) + ":" + TimeStamp
```

and carry `X-PARTNER-ID` (our DOKU client id), `X-EXTERNAL-ID` (a numeric reference unique within the day), `X-TIMESTAMP`, `X-SIGNATURE`, `CHANNEL-ID` and `Authorization: Bearer <accessToken>`.

Base URLs: `https://api-sandbox.doku.com` (sandbox) and `https://api.doku.com` (production).

> **Verification note.** The egress proxy in the authoring environment blocks `developers.doku.com`, `docs.doku.com` and `sandbox.doku.com`, so the table above is assembled from DOKU's public documentation index and secondary sources, not read off the live docs. **Phase 3 must confirm every path, header name, field name and response code against the DOKU dashboard for our own merchant account before any of it is hard-coded**, and the PR description must say which page each was confirmed from. Nothing downstream of the `PaymentGatewayRepository` port depends on these details being right, which is exactly why the port exists (D2).

---

## UX Flow

Mobile-first, 375 px, Bahasa Indonesia, `formatRupiah()` for money — all unchanged from `prd-table-ordering.md` (D15).

**1. Cart → Checkout.** The existing sticky **Checkout** button on `/t/{code}/cart`. Unchanged.

**2. Checkout summary (`/t/{code}/checkout`).** Read-only recap: table label, line items with their options and notes, and the total. One primary button, **"Bayar dengan QRIS · Rp X"**. This screen makes the price the guest is about to be charged explicit before a QR — and therefore an amount — is locked in.

**3. Name prompt.** Tapping the pay button opens a sheet asking for the guest's name ("Atas nama siapa pesanan ini?"), prefilled with the name this session used last time if there is one. One field, one **"Lanjutkan ke pembayaran"** button, and a cancel that returns to the summary. **No QR is generated until the name is confirmed** — the name is part of the checkout request, so a guest who abandons here has created nothing.

**4. QR displayed (same route, `awaitingPayment` state).** The button is replaced by:

- the QR itself, rendered client-side from `qrContent` via `react-native-qrcode-svg`, sized to fill the column width and with generous quiet space;
- the amount, large, above the QR;
- **"Simpan QR"**, a download button directly under the QR (D23) — the guest is paying on the *same* phone the QR is displayed on, so scanning it with a camera is impossible. They save the image and use their banking app's "QRIS dari galeri";
- a countdown to expiry ("Selesaikan pembayaran dalam 04:32");
- copy covering both routes: "Simpan QR lalu buka aplikasi bank atau e-wallet Anda, dan pilih bayar QRIS dari galeri. Atau pindai dengan perangkat lain.";
- a quiet, always-visible line: "Menunggu pembayaran…" with a subtle activity indicator.

The screen polls its own API for status. It never polls DOKU.

**5. Success.** The polling flips to paid. The screen shows a success state — check mark, "Pembayaran berhasil", the amount and the guest's name — and after ~2 seconds routes to the status screen. The delay is deliberate: an instant redirect makes a guest doubt the payment landed.

**6. Order being prepared (`/t/{code}/status?ref={reference}`).** "Pesanan Anda sedang disiapkan", the **table label** rendered large (it is how the order will find them — there is no pager and no order number, D16), the guest's name, the paid line items and total. Terminal — nothing polls, nothing refreshes, per the non-goals. A "Pesan lagi" button returns to the menu with a fresh, empty cart.

**7. Expiry.** The countdown reaches zero: "Waktu pembayaran habis". The cart is intact and unfrozen; one button, "Coba bayar lagi", returns to state 2 and mints a new QR. The name is already known by then, so the prompt is skipped on the retry.

**8. Failure.** A gateway error at QR generation shows `ErrorView` with a retry that re-issues the checkout call. Nothing has been charged and nothing has been created that the guest can see.

---

## Confirmed Product & Technical Decisions

| # | Decision | Choice | Rationale |
|---|---|---|---|
| **D1** | Where the gateway lives | **Entirely in `apps/api` (Go).** The order app calls only our own endpoints; DOKU credentials, signing keys and the notification URL exist only on the API host. | Required by the acceptance criteria, and already the assumption `prd-table-ordering.md` was written against ("the Go API owns it — gateway secrets and payment callbacks terminate at `apps/api`, never in the customer bundle"). A browser-visible DOKU credential is a credential anyone can mint QRs with. |
| **D2** | Gateway as a domain port | **Two ports, two files, both `*Repository`:** `domain/payment_repository.go` declares `PaymentRepository` (persistence), and `domain/payment_gateway_repository.go` declares **`PaymentGatewayRepository`** (the external service), each with its own `//go:generate mockgen` header. `data/doku/` holds the only code that knows what DOKU is. | Follows the codebase's actual convention rather than an imagined one. **Every port in the repo is named `*Repository` — 19 of 19 in `apps/api/domain`, 36 of 36 in `libs/ui/src/domain/repositories` — and the suffix does not mean "database":** `SessionRepository` is backed by a cookie and `localStorage`, `CartQueryRepository` and every `*ListQueryRepository` by the URL query string. A port backed by DOKU's HTTP API is the same kind of thing, and naming it `PaymentGateway` would make it the one port in the codebase that reads differently. Go is also strictly **one interface per file** (19 interfaces across 19 `*_repository.go` files), and `libs/ui` splits a second port on the same feature into its own file (`cart.ts` + `cartQuery.ts`) — so the gateway gets its own file, not a second declaration inside the persistence one. The compound name is unremarkable here next to `TransactionStatisticListQueryRepository`. |
| **D3** | DOKU API flavour | **SNAP Direct API**, QRIS MPM dynamic (`qr-mpm-generate` / `qr-mpm-query`), not DOKU Checkout and not the archived non-SNAP QRIS API. | SNAP is the Bank Indonesia standard DOKU documents as current; the archived non-SNAP QRIS endpoints are labelled as such in DOKU's own docs. Building on the archived surface would buy a rewrite. **Rejected:** DOKU Checkout — the acceptance criteria require our own UI. |
| **D4** | When the `Transaction` is created | **At QR generation, unpaid**, in the same DB write as the payment record. `PayTransaction` runs when DOKU confirms. | `transaction_items` **is** the price snapshot — it already stores `price`, `productName` and the option values at creation time, which is exactly what must be frozen when an amount is locked into a QR. Creating the transaction later would mean building a second snapshot table that duplicates it. Reusing the existing unpaid → paid lifecycle also means the wallet credit, MDR and `totalIncome` math is code that already ships and is already tested. **Rejected:** create-on-success — it either recomputes prices after the fact (charging one amount and recording another when staff edit a price mid-checkout) or duplicates `transaction_items`. **Accepted cost:** an abandoned checkout leaves an unpaid transaction; D5 disposes of it. |
| **D5** | Abandoned checkouts | When a payment expires, the payment row goes to `expired` **and its transaction is soft-deleted** (`deleted_at`), which `GetTransactionList` already excludes. A late `paid` notification for an expired payment un-deletes the transaction, pays it, and logs at `warn`. | Keeps the POS list free of orders nobody paid for, without inventing a transaction status. The un-delete path exists because "expired locally, paid at DOKU" is a real race, and the money is real — the payment record, not our timer, is the authority. `DeleteTransactionById` already refuses to delete a paid transaction, so the guard is in place. |
| **D6** | How staff tell the two apart | A `source` column on `transactions`: **`pos` \| `order`**, `NOT NULL DEFAULT 'pos'`, every existing row backfilled to `pos`. Surfaced as a badge on the list item and the detail screen, and as a filter alongside the existing payment-status and wallet filters. | Requested, with the values named as requested. A column beats inferring it from `cart_id IS NOT NULL` or from the wallet: it stays correct if a future channel also uses a cart, it indexes, and it reads plainly in SQL for reporting. Defaulting to `pos` makes the migration non-breaking for every existing row and for `TransactionCreate` on the POS, which does not send the field. **Note for review:** the stored value is `order`, but the badge staff read says "Order App" — "Order" alone next to a list of orders reads as a category, not an origin. Say the word if you want the badge to match the value literally. |
| **D7** | The order's table | `transactions.cart_id` (nullable FK) is stored alongside `source`, and the POS transaction response carries the resolved `table: PublicTable \| null`. | Without it an order-app transaction is undeliverable — staff would know an order was paid but not where to carry it. It is one nullable FK and one join through a cart that already carries `table_id`; storing `table_id` directly on the transaction would duplicate a fact the cart owns. |
| **D8** | Session ID as a capability, re-evaluated | The session ID stays the only credential, and the checkout routes stay `RequireSessionId`. What it now grants: creating a payment **for its own cart at a server-computed amount**, and reading that payment's status. It never grants spending, refunding, marking anything paid, or reading another session's payment. | D8 in `prd-table-ordering.md` required this re-evaluation. A stolen session ID can, at worst, generate a QR that the thief would then have to pay themselves and whose goods are delivered to the victim's table — not an attack, an act of charity. The state changes that move money (`paid`) are reachable **only** from a signature-verified DOKU notification or a DOKU status query, never from a client call. |
| **D9** | Charged amount authority | Computed server-side from the cart at checkout, in the same DB transaction that writes `transaction_items` and the payment row. No amount field is accepted from the client, on any route. | Continues D7 of `prd-table-ordering.md`. Once the QR exists the amount is immutable — a later price edit by staff cannot change what the guest is being asked to pay, and cannot change what the transaction records. |
| **D10** | Cart freeze | While a payment for a cart is `pending` and unexpired, every `/carts/current*` **write** returns `400` (`bad_request`, "cart is locked by a pending payment"). Reads are unaffected. `expired` or `failed` unlocks it; `paid` converts it. | The cart is the thing the amount was computed from. Letting a guest add an item to a cart whose QRIS QR is on screen produces an order that is either underpaid or mis-prepared. The freeze is the smallest correct answer, and it is naturally released by the expiry the guest can already see counting down. |
| **D11** | One QR per cart | `POST /carts/current/checkout` is idempotent: with a `pending`, unexpired payment on the active cart it returns **that** payment rather than minting a second one. | A double-tapped button or a reloaded screen must not produce two QRs for one order — two QRs means two possible payments for one set of drinks. |
| **D12** | Learning that payment succeeded | **Both**: DOKU's notification webhook is the primary path; the order app polls `GET /payments/{reference}` every 3 s while `pending`, and that read **re-queries DOKU** (`qr-mpm-query`) when the payment is still `pending` and its status was last checked more than 10 s ago. | A webhook we do not control is a single point of failure, and its absence is silent — the guest would stare at a paid QR and a spinner. The poll's re-query makes the webhook an optimisation rather than a dependency. The 10 s floor keeps a rapid client poll from becoming a rapid DOKU poll. |
| **D13** | Notification authentication | The route is unauthenticated but **signature-verified**: the symmetric HMAC-SHA512 scheme, recomputed over the received body, compared in constant time; a skewed or missing `X-TIMESTAMP` (> 5 min) is rejected. A rejected notification is `401` and is logged with the reference number. | It is the only unauthenticated write in the API, and it is the one that marks money received. Its own middleware (`VerifyDokuSignature`), sitting beside `CheckAuth` and `RequireSessionId`, keeps that fact legible in `*_route.go` instead of buried in a handler. |
| **D14** | Notification idempotency | The handler is keyed on `partner_reference_no`. A notification for a payment already `paid` is a no-op returning `200`. Transitions are one-way: `pending → paid \| expired \| failed`, and `expired → paid` only via D5. | DOKU retries until it sees a 200, so duplicates are expected traffic, not an error. Paying a transaction twice would credit the wallet twice — `PayTransaction` already guards with "transaction already paid", and this is the second lock. |
| **D15** | Which wallet the money lands in | A configured wallet id, `ORDER_PAYMENT_WALLET_ID`, pointed at the existing `QRIS` wallet. Validated at startup: unset, unknown, deleted, or `is_payment_target = false` fails the boot with a named error. | `PayTransaction` already credits a wallet and subtracts its `paymentCostPercentage`, which is precisely the DOKU MDR — so settlement accounting is configuration, not code. Failing at boot rather than at the first checkout means a misconfiguration is found by a deploy, not by a guest holding a phone. **Rejected:** matching a wallet by the name `"QRIS"` — a rename would silently redirect revenue. |
| **D16** | `orderNumber` for order-app transactions | **None.** Order-app transactions keep `order_number = 0`, the column's existing default. Nothing is assigned, and no sequence is shared with the counter. | Corrected from an earlier draft: the order number **is** the pager number, and a pager is something a cashier physically hands over — a guest ordering from their table never receives one, so a number for them would be meaningless at best and would collide with a real pager at worst. The existing UI needs no change to accommodate this: `TransactionListItem` already gates its ORDER NUMBER footer row on `isShown: orderNumber > 0` and `TransactionDetail` on `orderNumber > 0`, so a `0` simply renders nothing. The order is instead identified by its **table** (D7) — which is where the drink is going anyway. |
| **D17** | `Transaction.name` for order-app transactions | **The guest's own name, asked for at checkout.** The name sheet opens when they tap the pay button, *before* any QR is generated, prefilled from `customers` (D24) when this session has ordered before. Required, trimmed, 1–60 characters. | The column is exactly this — a customer name the POS list searches on ("Search Customer Name") and the order slip prints — so filling it with anything else (a table label, a placeholder) would mean staff searching a field that no longer holds what it says. Asking before QR generation, rather than after payment, means an abandoned prompt has created nothing at all: no transaction, no payment, no QR. The prefill is what keeps a second order to one tap. |
| **D18** | Where the payment reference lives in the URL | The status screen is `/t/{code}/status?ref={partnerReferenceNo}`, and `partnerReferenceNo` is `ORD` + 13 random Crockford base32 characters from the existing table-code generator — never a sequence. | The reference must survive the cart converting (after which `GET /carts/current` returns a fresh empty cart and can no longer reach the payment). Putting it in the URL makes reload and back work. Reads are still scoped by `X-Session-Id`, so the random reference is defence in depth, not the lock — but a sequential id in a URL invites enumeration and there is no reason to offer it. |
| **D19** | The webhook route in `api.yaml` | It is declared in `libs/api-contract/src/api.yaml` like every other route, tagged `payment`. The generated TS client function for it is never imported. | `api.yaml` is the single source of truth the Go models are generated from too; a hand-written request struct for the one route that parses untrusted input is the wrong place to break that rule. The unused client export tree-shakes away. |
| **D20** | Feature flag | `NEXT_PUBLIC_ORDER_CHECKOUT_ENABLED` survives and keeps its meaning as the kill switch: `false` renders "Checkout belum tersedia" exactly as today, `true` renders the real checkout. It stays `false` until phase 13. | The flag already exists, `CartScreen` already routes through it, and every phase before the last can therefore merge to `main` and deploy without a guest ever reaching a half-built checkout. |
| **D21** | Sandbox vs production | Environment-selected via `DOKU_BASE_URL` plus its own credential set. The sandbox is what CI, local development and manual QA use; production credentials exist only in the API host's systemd `EnvironmentFile`. | The DOKU merchant account has distinct credentials per environment; making the base URL configuration rather than a build flag means promoting to production is an env change and a dashboard webhook registration, not a code change. |
| **D22** | Where the checkout frontend code lives | `libs/ui`, under the existing order slices, reached through `@gatherloop-pos/ui/order` (D20 of `prd-table-ordering.md`). `apps/order-web` gains two thin pages. | Unchanged from every other order-app slice. No new library, no new entry point. |
| **D23** | Saving the QR | A **"Simpan QR"** button under the QR downloads it as a PNG (`qris-{reference}.png`). `react-native-qrcode-svg`'s `toDataURL` callback yields base64, which becomes a `Blob` + `URL.createObjectURL` and an `<a download>` click; the object URL is revoked after. If `download` is unsupported (older iOS Safari), the fallback opens the PNG in a sheet with "Tekan dan tahan gambar untuk menyimpan". | **The guest is paying on the same phone the QR is displayed on** — they cannot point its camera at its own screen. Without this the flow only works for a guest with a second device, which is nobody. Every Indonesian banking and e-wallet app supports paying a QRIS image from the gallery, so a saved PNG is a complete payment path. The fallback exists because a silently inert download button on iOS would break the feature for roughly half the guests. |
| **D24** | Remembering the guest's name | A `customers` table keyed by `session_id` (unique), holding just `name`. Read at SSR to prefill the prompt; upserted as part of the checkout call. | The name is an attribute of the anonymous session, not of a cart or a transaction — a cart is discarded at conversion, and a transaction is a historical record that must keep the name it was paid under even if the guest later corrects their spelling. A dedicated table is also what makes "prefilled for the next order" a lookup rather than a scan back through converted carts. **Deliberately not a CRM:** no phone, no email, no marketing consent, no cross-session identity — a row is a display name and a session ID, and it inherits the session cookie's one-year life. **Rejected:** `carts.customer_name` — it loses the name every time a cart converts, which is precisely when we learned it. |

---

## Feature Requirements

### FR-1 — `source` and `cart_id` on `Transaction` (API)

Migration `000023_add_transaction_source` (next free number; latest is `000022_add_product_variant_recipe`):

```sql
ALTER TABLE `transactions`
  ADD COLUMN `source`  VARCHAR(16) NOT NULL DEFAULT 'pos' AFTER `name`,
  ADD COLUMN `cart_id` BIGINT      NULL                   AFTER `source`,
  ADD KEY `idx_transactions_source` (`source`),
  ADD KEY `idx_transactions_cart_id` (`cart_id`),
  ADD CONSTRAINT `fk_transactions_cart` FOREIGN KEY (`cart_id`) REFERENCES `carts` (`id`);
```

`VARCHAR(16)` with a plain string value matches how `saleType`, `station` and `carts.status` are already modelled. The `.down.sql` drops the constraint, the keys, then the columns.

- `domain.Transaction` gains `Source TransactionSource` (`TransactionSourcePos` = `"pos"` / `TransactionSourceOrder` = `"order"`) and `CartId *int64`, `Cart *Cart`.
- `TransactionRequest` in `api.yaml` is **unchanged** — the POS never sends a source, and `CreateTransaction` defaults an empty source to `pos`, so the existing POS create path is untouched.
- `Transaction` in `api.yaml` gains required `source` (`enum: [pos, order]`) and optional `table: PublicTable`.
- `GET /transactions` gains an optional `source` query parameter (`pos` \| `order` \| `all`, default `all`), threaded through `GetTransactionList` / `GetTransactionListTotal` the way `paymentStatus` and `walletId` already are.
- The restapi transformer resolves `table` from `cart_id → carts.table_id → tables` and returns `null` for POS transactions.

Handler tests assert: an existing (`pos`) transaction serialises as `source: "pos"`; the `source=order` filter excludes POS rows; `POST /transactions` without a source still creates a `pos` transaction.

### FR-2 — POS surfaces the source (frontend)

- `TransactionListItem` renders a badge when `source === 'order'` — label "Order App" (D6) — and the table label from `table` when present. POS transactions render no badge, so the existing list is visually unchanged. Its ORDER NUMBER footer row already gates on `isShown: orderNumber > 0`, so order-app rows drop it with no code change (D16).
- `TransactionList`'s filter popover gains a **Source** radio group (`All` / `POS` / `Order App`) beside the existing wallet and payment-status controls.
- `transactionList` usecase params and the `transactionListQuery` URL repository carry `source`, so the filter is bookmarkable exactly as the existing filters are.
- `TransactionDetail` shows a Source row, and the table label when present. Its order-number row already gates on `orderNumber > 0` and needs no change.
- Stories for both new states on `TransactionListItem` and `TransactionDetail`; mobile parity via the shared components (`pos-mobile` renders the same list item).

### FR-3 — Payment gateway port and DOKU client (API)

The `payment` feature follows the repo's domain file convention exactly — `payment_entity.go`, `payment_usecase.go`, and **one file per port**, each with its own `//go:generate mockgen` header (D2):

| File | Declares | Implemented by | Mock |
|---|---|---|---|
| `domain/payment_repository.go` | `PaymentRepository` | `data/mysql` | `data/mock/payment_repository.go` |
| `domain/payment_gateway_repository.go` | `PaymentGatewayRepository` | `data/doku` | `data/mock/payment_gateway_repository.go` |

`domain/payment_entity.go`:

```go
type QrisPayment struct {
    PartnerReferenceNo string
    GatewayReferenceNo string
    QrContent          string
    ExpiredAt          time.Time
}

type QrisStatus struct {
    PartnerReferenceNo string
    GatewayReferenceNo string
    Status             PaymentGatewayStatus // pending | paid | expired | failed
    PaidAmount         float32
    RawStatusCode      string
}

`domain/payment_gateway_repository.go`:

```go
//go:generate mockgen -source=payment_gateway_repository.go -destination=../data/mock/payment_gateway_repository.go -package=mock

// PaymentGatewayRepository is the port to the payment provider. It is named
// like every other port in the repo: the `Repository` suffix marks a domain
// -declared interface implemented in an outer layer, not a database —
// SessionRepository is a cookie, CartQueryRepository is the URL (D2).
type PaymentGatewayRepository interface {
    GenerateQris(ctx context.Context, input GenerateQrisInput) (QrisPayment, *Error)
    QueryQris(ctx context.Context, input QueryQrisInput) (QrisStatus, *Error)
    VerifyNotificationSignature(method, path string, headers NotificationHeaders, body []byte) *Error
    ParseNotification(body []byte) (QrisStatus, *Error)
}
```

`data/doku/` — a new sibling of the existing `data/mysql` and `data/mock` — implements it:

- `client.go` — base URL, HTTP client with an explicit 10 s timeout, structured logging of every call with the reference number and **never** the signature, secret, private key or access token.
- `token.go` — the B2B access token, cached in memory with a margin (refresh at `expiresIn - 60s`), guarded by a mutex, refetched once on a `401`.
- `signature.go` — the asymmetric (`SHA256withRSA`) and symmetric (`HMAC-SHA512`) schemes, plus the JSON minifier the symmetric digest is computed over. Signature verification uses `hmac.Equal`.
- `qris.go` — `qr-mpm-generate` and `qr-mpm-query`, mapping DOKU response codes to `PaymentGatewayStatus` and DOKU errors to `domain.Error`.

Tests are `httptest`-based and cover: token caching and refresh; a signature computed against a fixed vector; `2004700`-shaped success mapped to `QrisPayment`; `latestTransactionStatus` `"00"` → `paid`, expiry/failure codes → `expired` / `failed`, anything unknown → `pending` (never optimistically `paid`); a notification with a tampered body rejected; a notification older than 5 minutes rejected.

Configuration in `utils/env.go` (all read once, all required when checkout is enabled): `DOKU_BASE_URL`, `DOKU_CLIENT_ID`, `DOKU_CLIENT_SECRET`, `DOKU_PRIVATE_KEY` (PEM), `DOKU_MERCHANT_ID`, `DOKU_CHANNEL_ID`, `DOKU_QRIS_EXPIRY_SECONDS` (default 900), `ORDER_PAYMENT_WALLET_ID`.

### FR-4 — `customers`: the guest's name (API)

Migration `000024_create_customers` — the store behind D24:

```sql
CREATE TABLE IF NOT EXISTS `customers` (
    `id`         BIGINT       NOT NULL AUTO_INCREMENT,
    `session_id` CHAR(36)     NOT NULL,
    `name`       VARCHAR(60)  NOT NULL,
    `created_at` DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP,
    `updated_at` DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    `deleted_at` DATETIME     NULL,
    PRIMARY KEY (`id`),
    UNIQUE KEY `uq_customers_session_id` (`session_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

`VARCHAR(60)` matches the validation ceiling, so a too-long name is rejected by the usecase rather than truncated by MySQL. No FK to `carts` — the row outlives any individual cart, which is the whole point (D24).

`domain/customer_{entity,repository,usecase}.go` + generated mock, MySQL repo + transformer, and one read route:

| Method | Path | Behaviour |
|---|---|---|
| `GET` | `/customers/current` | Returns `{ name }` for the session, or `{ name: "" }` if this session has never given one. Never `404` — "no name yet" is the expected first-visit state, not an error, exactly as `GET /carts/current` never 404s. |

`RequireSessionId` guards it. The write is not a route of its own: the name is upserted as part of checkout (FR-6), so there is no way to record a name without an order attached to it.

`UpsertCustomerName` trims, rejects empty and rejects > 60 characters with `400`. Usecase tests cover first-name, name-change, whitespace-only rejection and the read of an unknown session.

### FR-5 — `payments` (API)

Migration `000025_create_payments`:

```sql
CREATE TABLE IF NOT EXISTS `payments` (
    `id`                   BIGINT       NOT NULL AUTO_INCREMENT,
    `cart_id`              BIGINT       NOT NULL,
    `session_id`           CHAR(36)     NOT NULL,
    `transaction_id`       BIGINT       NULL,
    `partner_reference_no` VARCHAR(64)  NOT NULL,
    `gateway_reference_no` VARCHAR(64)  NOT NULL DEFAULT '',
    `method`               VARCHAR(16)  NOT NULL DEFAULT 'qris',
    `status`               VARCHAR(16)  NOT NULL DEFAULT 'pending',
    `amount`               FLOAT        NOT NULL DEFAULT 0,
    `qr_content`           TEXT         NULL,
    `expired_at`           DATETIME     NOT NULL,
    `paid_at`              DATETIME     NULL,
    `status_checked_at`    DATETIME     NULL,
    `created_at`           DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP,
    `updated_at`           DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    `deleted_at`           DATETIME     NULL,
    PRIMARY KEY (`id`),
    UNIQUE KEY `uq_payments_partner_reference_no` (`partner_reference_no`),
    KEY `idx_payments_cart_id_status` (`cart_id`, `status`),
    KEY `idx_payments_session_id` (`session_id`),
    CONSTRAINT `fk_payments_cart`        FOREIGN KEY (`cart_id`)        REFERENCES `carts`        (`id`),
    CONSTRAINT `fk_payments_transaction` FOREIGN KEY (`transaction_id`) REFERENCES `transactions` (`id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

Notes: no raw-notification blob column — the notification is logged, not stored, because it can carry payer identifiers we have no purpose for and no retention policy about. `status_checked_at` is what implements D12's 10 s floor. `method` exists so a second method is a value, not a migration; only `qris` is ever written.

**What a `payments` row is not:** it records a *gateway* payment attempt, so the table is not a ledger of all money received. A cashier taking cash or card on the POS still produces nothing here — that payment lives, as it always has, in `transactions.paid_at` / `wallet_id`. Any reporting that wants "everything paid" reads `transactions`, not `payments`.

Domain entity `Payment` in `payment_entity.go` and `PaymentRepository` in its own `payment_repository.go` (D2), MySQL repo + transformer, repo tests — following the `cart` slice exactly.

### FR-6 — Checkout endpoints (API)

All session routes require a valid `X-Session-Id` (`RequireSessionId`, unchanged).

| Method | Path | Body | Behaviour |
|---|---|---|---|
| `POST` | `/carts/current/checkout` | `{ customerName }` | Upserts the name, creates the transaction (unpaid) + payment (pending), returns the payment with `qrContent`. Idempotent per D11. |
| `GET` | `/payments/{partnerReferenceNo}` | — | Returns the payment. `404` unless `payment.session_id` matches the header. Re-queries DOKU per D12. |
| `POST` | `/payments/doku/notification` | DOKU SNAP notification | Signature-verified (D13), idempotent (D14). Always `200` once the signature is valid. |

`POST /carts/current/checkout`, in one DB transaction:

1. Validate `customerName` (trimmed, 1–60 chars) ⇒ `400` otherwise. Upsert it into `customers` for this session (FR-4).
2. Load the session's active cart. Empty cart ⇒ `400` "cart is empty". No table attached ⇒ `400` "table is not set".
3. A `pending`, unexpired payment on this cart ⇒ return it and stop (D11). The name upsert in step 1 still applies, so a guest who corrects their name and retries gets the correction saved even though the QR is reused.
4. Compute the total from current variant prices (D9).
5. `CreateTransaction` with `source = order`, `cartId`, `name = customerName` (D17), **`orderNumber = 0`** (D16), one `TransactionItem` per cart line (`variantId`, `amount`, `note`, `discountAmount: 0`), `transactionCoupons: []`. This snapshots price, product name and option values.
6. Mint `partnerReferenceNo` (D18); insert the payment row `pending` with `expired_at = now + DOKU_QRIS_EXPIRY_SECONDS`.
7. Call `PaymentGatewayRepository.GenerateQris`. On error the whole DB transaction rolls back — no orphan transaction, nothing to reconcile — and the endpoint returns `502` `internal_server_error`.
8. Store `qrContent` and `gatewayReferenceNo`; return the payment.

The cart is **not** converted here — only a paid payment converts it (D10).

`GET /payments/{partnerReferenceNo}`: if `status = pending` and `status_checked_at` is null or older than 10 s, call `QueryQris` and apply the result through the same state transition used by the notification handler, so both paths share one code path and one set of guards. Response carries `partnerReferenceNo`, `status`, `amount`, `qrContent`, `expiredAt`, `paidAt`, `customerName`, `tableLabel`, and the paid line items — enough for the status screen to render without a second call. No order number: there is none (D16).

`POST /payments/doku/notification`:

1. `VerifyDokuSignature` middleware. Invalid ⇒ `401`, logged with the reference.
2. Parse to a `QrisStatus`; unknown `partnerReferenceNo` ⇒ `200` with a `warn` log (never `404` — a `404` makes DOKU retry a notification we can never handle).
3. `paid` and payment already `paid` ⇒ `200`, no-op (D14).
4. `paid` and payment `pending` or `expired` ⇒ in one DB transaction: un-delete the transaction if D5 soft-deleted it; `PayTransaction(walletId = ORDER_PAYMENT_WALLET_ID, paidAmount = payment.amount, transactionId)`; set the payment `paid`, `paid_at`; set the cart `converted`.
5. `expired` / `failed` ⇒ payment status set, transaction soft-deleted, cart left `active` and thereby unfrozen.
6. `200` in every case where the signature was valid.

**A mismatched amount is a hard stop.** If the notification's amount differs from `payment.amount`, nothing is paid: the payment is left `pending`, the event is logged at `error` with both amounts, and the response is still `200`. Staff reconcile by hand. Silently accepting a different amount than the one we recorded is how a reconciliation bug becomes a revenue bug.

### FR-7 — Cart freeze (API)

`CartUsecase.AddCartItem`, `UpdateCartItem`, `RemoveCartItem`, `ClearCart` and `UpdateCartTable` first check for a `pending`, unexpired payment on the active cart and return `400` `bad_request` "cart is locked by a pending payment" if one exists. `GetCurrentCart` is unaffected.

Usecase tests cover each mutation against a locked and an unlocked cart, and that an expired pending payment does not lock.

### FR-8 — Frontend payment slice (domain + data)

Per the file inventory in `prd-table-ordering.md`:

- `domain/entities/Payment.ts` — `{ reference, status: 'pending' | 'paid' | 'expired' | 'failed', amount, qrContent, expiredAt, paidAt, customerName, tableLabel, items }`.
- `domain/repositories/payment.ts` — `PaymentRepository { checkout(customerName): Promise<Payment>; fetchPayment(reference): Promise<Payment> }`.
- `domain/repositories/customer.ts` — `CustomerRepository { fetchCurrentName(): Promise<string> }`, for the prefill (D24).
- `data/api/{payment,customer}.ts` + transformers, and `data/mock/{payment,customer}.ts`. Each API repository takes a `SessionRepository` and attaches `X-Session-Id` with `withCredentials: false`, exactly as `ApiCartRepository` does.
- `domain/usecases/checkout.ts` — the state machine. The name is machine state, not screen state: it is validated, submitted and echoed back, so it belongs with the transitions it gates.

```ts
type Context = {
  payment: Payment | null;
  customerName: string;      // seeded from GET /customers/current at SSR
  nameErrorMessage: string | null;
  errorMessage: string | null;
};

export type CheckoutState = (
  | { type: 'idle' }              // summary shown, nothing requested yet
  | { type: 'askingName' }        // name sheet open, no QR yet (D17)
  | { type: 'creatingPayment' }   // POST /carts/current/checkout in flight
  | { type: 'awaitingPayment' }   // QR on screen, polling
  | { type: 'paid' }
  | { type: 'expired' }
  | { type: 'error' }
) & Context;

export type CheckoutAction =
  | { type: 'ASK_NAME' }
  | { type: 'CHANGE_NAME'; name: string }
  | { type: 'CANCEL_NAME' }
  | { type: 'SUBMIT_NAME' }      // validates, then enters creatingPayment
  | { type: 'CHECKOUT_SUCCESS'; payment: Payment }
  | { type: 'CHECKOUT_ERROR'; message: string }
  | { type: 'POLL' }
  | { type: 'POLL_SUCCESS'; payment: Payment }
  | { type: 'POLL_ERROR'; message: string }
  | { type: 'EXPIRE' };
```

`SUBMIT_NAME` on an empty or over-long trimmed name stays in `askingName` and sets `nameErrorMessage` — the reducer is where that rule lives, so the screen renders the error rather than deciding it. Retry from `expired` goes straight to `creatingPayment`, skipping `askingName`, because the name is already in context (UX step 7).

`onStateChange` owns the 3 s poll while `awaitingPayment` and clears it on every other state. A `POLL_ERROR` does **not** leave `awaitingPayment` — a dropped poll on a café network must not tell a guest their payment failed; it is logged and the next tick retries. Only an explicit `expired`/`failed` status, or the local countdown reaching `expiredAt`, leaves the state.

`checkout.test.ts` covers, via `UsecaseTester` with `MockPaymentRepository`: `idle → askingName → creatingPayment → awaitingPayment`; an empty and an over-long name each held at `askingName` with an error; `CANCEL_NAME` returning to `idle` having created nothing; a seeded name still requiring an explicit submit; `awaitingPayment → paid`; a poll error keeping `awaitingPayment`; `awaitingPayment → expired` on both the server status and the local countdown; `expired → creatingPayment` skipping the prompt; `creatingPayment → error` and retry.

### FR-9 — Checkout screen (frontend)

`CheckoutScreen` gains a `variant` prop mapped exhaustively in `CheckoutHandler` with `match(...).returnType<CheckoutScreenProps['variant']>()...exhaustive()`, replacing today's `enabled` boolean while keeping the flag's `disabled` variant (D20). Variants: `disabled`, `summary`, `askingName`, `creatingPayment`, `awaitingPayment`, `paid`, `expired`, `error`.

New dumb components under `presentation/views/components/checkout/`, each with stories: `CheckoutSummaryView` (recap + pay button), `CustomerNameSheet` (one field, prefilled, inline validation error, continue + cancel), `QrisPaymentView` (QR + amount + save button + countdown + waiting indicator), `PaymentSuccessView`. The QR renders through `react-native-qrcode-svg`.

**Saving the QR (D23).** `QrisPaymentView` holds a ref to the QR and, on "Simpan QR", calls its `toDataURL` callback, converts the base64 to a `Blob`, and clicks a synthesised `<a download="qris-{reference}.png">`, revoking the object URL afterwards. The button is feature-detected: where `download` is unsupported the same PNG opens in a sheet with "Tekan dan tahan gambar untuk menyimpan". This path is DOM-only and web-only, which is correct — the order app is web-only — but it means the component must not assume the DOM at module scope, so the download helper lives in `libs/ui/src/utils/` behind a guard and is unit-tested there rather than in the component.

**Manual verification is required on real devices, not just Storybook:** iOS Safari and Android Chrome, saving the QR and paying the saved image from at least one banking app's QRIS-from-gallery flow. A download button that silently does nothing is indistinguishable from a working one in a story.

The handler routes to `/t/{code}/status?ref={reference}` ~2 s after `paid`, and back to `/t/{code}/cart` from the expired state's secondary action.

`CheckoutHandler.test.tsx` covers each variant against mock repositories, plus the name sheet opening prefilled, a rejected name keeping the sheet open with nothing created, the redirect on `paid`, and the retry on `error`.

### FR-10 — Order status screen (frontend)

`OrderStatusScreen` + `OrderStatusHandler` + `app/order/OrderStatus.tsx` + `apps/order-web/src/pages/t/[code]/status.tsx`. `getServerSideProps` resolves session and table as the other order pages do, and seeds the payment by reference so the first paint is the real order and not a spinner. States: loaded (table label large, customer name, items, total, "Pesanan Anda sedang disiapkan"), not-found (unknown or foreign reference — `EmptyView`, back to the menu), error (`ErrorView` with retry). Nothing polls.

### FR-11 — Copy

All customer-facing copy is Bahasa Indonesia (D15 of `prd-table-ordering.md`), money through `formatRupiah()`. Staff-facing POS strings stay English, matching the rest of the POS.

---

## Configuration and Secrets

| Variable | Where | Notes |
|---|---|---|
| `DOKU_BASE_URL` | `apps/api/.env` | `https://api-sandbox.doku.com` or `https://api.doku.com`. |
| `DOKU_CLIENT_ID` | `apps/api/.env` | Sent as `X-PARTNER-ID` / `X-CLIENT-KEY`. |
| `DOKU_CLIENT_SECRET` | `apps/api/.env` | Symmetric signing key. **Secret.** |
| `DOKU_PRIVATE_KEY` | `apps/api/.env` | PEM, newline-escaped. Asymmetric signing key. **Secret.** |
| `DOKU_MERCHANT_ID` | `apps/api/.env` | Required by `qr-mpm-query`. |
| `DOKU_CHANNEL_ID` | `apps/api/.env` | `CHANNEL-ID` header. |
| `DOKU_QRIS_EXPIRY_SECONDS` | `apps/api/.env` | Default `900`. Drives both the QR and the local countdown. |
| `ORDER_PAYMENT_WALLET_ID` | `apps/api/.env` | The `QRIS` wallet (D15). Validated at boot. |

`.env.example` gains all eight with empty values and comments. **No DOKU variable is ever added to `apps/order-web` or any Vercel project** — a `NEXT_PUBLIC_` DOKU key would be a published credential.

The notification URL (`https://<api-host>/payments/doku/notification`) is registered in the DOKU dashboard per environment. That registration is a deployment step, not code; phase 13's checklist names it.

---

## Non-Functional Requirements

| Area | Requirement |
|---|---|
| Secrecy | No DOKU secret, private key, access token or signature is ever logged, returned in an error body, or sent to any frontend. Log lines carry `partnerReferenceNo` and status only. |
| Latency | Checkout (`POST /carts/current/checkout`) completes in < 3 s at p95, including the DOKU round trip. Outbound DOKU calls time out at 10 s. |
| Correctness | A guest is never charged twice for one cart (D11), and one payment never pays two transactions (D14). A payment is `paid` only on a signature-verified notification or a DOKU status query — never on client input. |
| Availability | A DOKU outage degrades to a visible error at QR generation. Nothing is created and nothing is charged. A webhook outage alone is invisible to the guest, because polling re-queries DOKU (D12). |
| Observability | Structured logs at each state transition; `warn` on an unknown reference or a late notification for an expired payment; `error` on a signature failure or an amount mismatch. |
| Testing | Go usecases against `mockgen` mocks; the DOKU client against `httptest`; frontend usecases through `UsecaseTester` with mock repositories; handlers through Testing Library; screens through Storybook; the whole flow through Playwright with a stubbed gateway. |
| Data | No cardholder data, no payer identity, and no notification body is persisted. `payments` holds only what reconciliation needs, and `customers` holds a display name and nothing else. |

---

## Success Metrics

1. **Checkout completion rate** — payments reaching `paid` ÷ payments created. Target > 80% within two weeks of enabling the flag; below that, the QR presentation or the expiry window is wrong.
2. **Orphan rate** — payments `paid` at DOKU whose transaction is not `paid` in our DB. Target **zero**; any occurrence is a P1.
3. **Webhook dependency** — share of payments confirmed by the poll's DOKU re-query rather than by the notification. A high share means the webhook registration or delivery is broken even though guests are unaffected.
4. **Counter load** — order-app share of daily transactions (`source = order`), the number the whole feature exists to move.

---

## Risks and Mitigations

| Risk | Mitigation |
|---|---|
| The DOKU API details in this document are wrong (docs unreachable while authoring) | Everything DOKU-shaped is behind `PaymentGatewayRepository` (D2) and lands in one phase (3) whose acceptance criterion is a successful sandbox round trip. A wrong path or field name costs that PR, not the design. |
| The notification never arrives (misregistered URL, DOKU outage, our host unreachable) | Polling re-queries DOKU (D12), so confirmation does not depend on it. Metric 3 makes silent breakage visible. |
| Notification arrives after we expired the payment locally | D5's un-delete-and-pay path, logged at `warn`. The payment record, not our timer, is the authority. |
| Guest pays, then our DB write fails | The payment stays `pending` and both the next poll and any notification retry re-attempt the same idempotent transition (D14). A permanent failure surfaces as a nonzero metric 2 and is reconciled from the DOKU dashboard by reference. |
| A price changes between QR generation and payment | The amount and the line prices are frozen in `transaction_items` at generation (D4/D9). The guest pays what they were shown. |
| Cart frozen and never released (a payment stuck `pending` because DOKU never answers) | `expired_at` is set at generation and always in the past eventually; the freeze check treats an expired pending payment as unlocked (FR-6), so the cart releases without any sweeper running. |
| Wallet misconfiguration books revenue to the wrong wallet | Boot-time validation of `ORDER_PAYMENT_WALLET_ID` against existence, soft-deletion and `is_payment_target` (D15). |
| **The QR download silently fails on a guest's browser, leaving them unable to pay at all** | The highest-severity UX risk in the feature, because the guest cannot scan a QR shown on the phone they are holding. Feature-detected fallback to a long-press-to-save sheet (D23), and phase 11 is not done until it is verified on real iOS Safari and real Android Chrome — a Storybook story cannot catch this. |
| Guests type junk names ("a", "asdf"), degrading the POS list | Accepted for v1: the name is a convenience for staff calling an order, not an identity. Validation is length-only — rejecting "unrealistic" names would reject real ones. The table label (D7) is the authoritative delivery target, so a junk name costs nothing operationally. |
| Abuse: someone mints QRs in a loop | Each QR is scoped to one cart with one pending payment (D11), so the ceiling is one QR per session per expiry window. Rate limiting remains out of scope (D12 of `prd-table-ordering.md`), still noted. |
| MDR mis-booked, understating income | `paymentCostPercentage` on the configured wallet is the single MDR input, and `PayTransaction` already applies it. Verify the configured percentage against the DOKU merchant agreement in phase 13. |

---

## Open Questions

1. **DOKU merchant account** — do we hold production credentials, or sandbox only? Phase 3 can complete on sandbox alone; phase 13 cannot.
2. **QR expiry window** — 15 minutes is the assumed default. Is a shorter window (5 min) better for table turnover, given that expiry unfreezes the cart and re-checkout is one tap?
3. **MDR** — what `paymentCostPercentage` does our DOKU QRIS agreement imply, and is the existing `QRIS` wallet already set to it?
4. **Settlement timing** — DOKU settles T+1 or later, but `PayTransaction` credits the wallet immediately. Is same-day wallet balance the behaviour finance wants, or should order-app revenue land in a "DOKU (belum settle)" wallet and be transferred on settlement? This changes only `ORDER_PAYMENT_WALLET_ID` and a manual transfer habit, so it does not block any phase.
5. **Minimum order amount** — QRIS has practical floors per issuer. Should checkout reject a cart below some threshold with a clear message rather than surfacing a gateway error?

---

## Implementation Phases

Thirteen PRs. Each is independently mergeable and deployable; nothing before phase 13 is reachable by a guest, because `NEXT_PUBLIC_ORDER_CHECKOUT_ENABLED` stays `false` (D20).

### Phase 1 — `source` and `cart_id` on transactions (API)

**Deliver:** migration `000023`, `domain.Transaction.Source` / `CartId` / `Cart`, MySQL entity + transformer, `api.yaml` (`Transaction.source`, `Transaction.table`, the `source` list filter), restapi transformer, `source` threaded through `GetTransactionList` / `GetTransactionListTotal`, regenerated TS + Go clients.
**Tests:** usecase tests for the filter; handler tests for serialisation, the filter, and a POS create defaulting to `pos`.
**Done when:** every existing transaction reads `source: "pos"`, the POS create path is byte-for-byte unchanged in behaviour, and `?source=order` returns nothing.
**Not in scope:** any UI.

### Phase 2 — POS shows and filters by source (frontend)

**Deliver:** FR-2 in full — badge, filter, detail row, usecase params, URL query repository, stories.
**Tests:** handler test for the filter round-tripping through the URL; stories for badge present/absent.
**Done when:** a manually inserted `order` row is visibly distinguishable and filterable on web and mobile.

### Phase 3 — DOKU client behind a domain port (API)

**Deliver:** FR-3 in full — `domain/payment_entity.go` and `domain/payment_gateway_repository.go` declaring `PaymentGatewayRepository` with its own mockgen header (D2), the generated mock, `data/doku/**`, env configuration, `.env.example`. **Wired into nothing.**
**Tests:** `httptest` coverage of token caching/refresh, both signature schemes against fixed vectors, generate/query response mapping, notification verification including tampering and clock skew.
**Done when:** a scratch `main` (not committed) generates a real QR against the DOKU sandbox and queries its status, and the PR description cites the DOKU documentation page confirming each path and field name.

### Phase 4 — `customers`: the guest's name (API)

**Deliver:** FR-4 — migration `000024`, `domain/customer_{entity,repository,usecase}.go` + generated mock, MySQL repo + transformer, `GET /customers/current` route + handler + transformer, `api.yaml` path and schema. The upsert is exposed as a usecase method only; nothing writes it yet.
**Tests:** usecase tests for read-unknown-session, first name, name change, whitespace-only and over-60-character rejection; handler test for the missing `X-Session-Id` case.
**Done when:** a session with no name reads `{ name: "" }` rather than a `404`.

### Phase 5 — `payments` model (API)

**Deliver:** FR-5 — migration `000025`, `domain/payment_repository.go` declaring `PaymentRepository` with its own mockgen header (D2), generated mock, MySQL repo + transformer, `api.yaml` schemas (`Payment`, `PaymentResponse`). Phase 3 already added `payment_entity.go`; this phase extends it with the `Payment` entity. **No routes.**
**Tests:** repository tests following the `cart` repo's shape, including the unique-reference constraint.
**Done when:** the migration applies and rolls back cleanly and the repo round-trips a payment.

### Phase 6 — Checkout endpoint (API)

**Deliver:** `PaymentUsecase.Checkout` (FR-6 steps 1–8) including the D17 name upsert, the `POST /carts/current/checkout` route + handler + transformer, `api.yaml` path, boot-time `ORDER_PAYMENT_WALLET_ID` validation, `main.go` wiring.
**Tests:** usecase tests against the mock gateway — empty cart, no table, missing/invalid `customerName`, idempotent re-checkout (including the name still being updated on the reused QR), gateway failure rolling everything back, price snapshot correctness, and `orderNumber` staying `0` (D16); handler tests for the missing/invalid `X-Session-Id` cases.
**Done when:** a `POST` carrying a name, for a session with a non-empty carted table, returns a `qrContent` and leaves exactly one unpaid `order` transaction named after the guest, one pending payment, and one `customers` row.

### Phase 7 — Cart freeze (API)

**Deliver:** FR-7 — the pending-payment guard on all five cart mutations.
**Tests:** each mutation locked and unlocked; an expired pending payment not locking.
**Done when:** a cart with a live QR rejects writes and accepts reads.

### Phase 8 — DOKU notification webhook (API)

**Deliver:** `VerifyDokuSignature` middleware, `POST /payments/doku/notification` route + handler, `ConfirmPayment` usecase (FR-6's notification steps 1–6, including D5's un-delete, D14's idempotency and the amount-mismatch stop), `api.yaml` path (D19).
**Tests:** valid notification pays the transaction, converts the cart and credits the wallet; a duplicate is a no-op `200`; a tampered signature is `401`; an unknown reference is `200` + `warn`; an amount mismatch pays nothing and logs `error`; an expired-then-paid payment un-deletes and pays.
**Done when:** replaying a captured sandbox notification produces exactly one paid transaction, whatever the number of replays.

### Phase 9 — Payment status read and expiry (API)

**Deliver:** `GET /payments/{partnerReferenceNo}` with session scoping and the D12 re-query (sharing the transition code path with phase 8's notification handler), the expiry transition (payment `expired`, transaction soft-deleted, cart unfrozen), `api.yaml` path.
**Tests:** foreign session ⇒ `404`; a `pending` payment past `expired_at` transitions on read; the 10 s floor prevents a re-query storm; a DOKU `paid` result reached by query pays exactly as the notification does.
**Done when:** the whole backend flow works with the webhook route disabled — proving the webhook is an optimisation, not a dependency.

### Phase 10 — Frontend payment slice (domain + data)

**Deliver:** FR-8 in full — `Payment` and `Customer` entities, both repository ports, API + mock repositories and transformers, `checkout` usecase. **No UI.**
**Tests:** `checkout.test.ts` covering every transition listed in FR-7, including a poll error keeping `awaitingPayment` and the local countdown expiry.
**Done when:** the machine is green headlessly, with no React and no network.

### Phase 11 — Checkout screens (frontend)

**Deliver:** FR-9 — `CheckoutScreen` variants replacing the stub body, `CheckoutSummaryView` / `CustomerNameSheet` / `QrisPaymentView` / `PaymentSuccessView` with stories, the D23 download helper in `libs/ui/src/utils/` behind a DOM guard, `CheckoutHandler` rewritten, updated `app/order/Checkout.tsx` and the checkout page's `getServerSideProps` seeding the known name.
**Tests:** handler tests per variant, the name sheet opening prefilled, a rejected name creating nothing, the `paid` redirect and the `error` retry; a story per variant; unit tests for the download helper's base64 → Blob path and its unsupported-`download` fallback.
**Done when:** with the flag on locally and the API pointed at the DOKU sandbox, a save-QR-then-pay-from-gallery round trip renders success — **verified on a real iOS Safari and a real Android Chrome, not only in Storybook** (D23) — and with the flag off the screen is exactly today's "Checkout belum tersedia".

### Phase 12 — Order status screen (frontend)

**Deliver:** FR-10 — `OrderStatusScreen` + stories, `OrderStatusHandler` + test, `app/order/OrderStatus.tsx`, `apps/order-web/src/pages/t/[code]/status.tsx` with `getServerSideProps` seeding the payment by reference, and the ~2 s redirect from phase 11's `paid` state wired to it.
**Tests:** handler tests for loaded, not-found (foreign or unknown reference) and error; a story per state.
**Done when:** paying in the sandbox lands on "Pesanan Anda sedang disiapkan" showing the table and the guest's name, and a reload of that URL still renders it.

### Phase 13 — Enable, verify, document

**Deliver:** `apps/order-web-e2e` coverage of cart → checkout → QR → paid → status against a stubbed gateway; production DOKU credentials in the API host's systemd `EnvironmentFile`; the notification URL registered in the DOKU dashboard; `ORDER_PAYMENT_WALLET_ID` and the `QRIS` wallet's `paymentCostPercentage` verified against the merchant agreement (open question 3); `NEXT_PUBLIC_ORDER_CHECKOUT_ENABLED=true`; a `docs-site/sales/` page for the checkout flow and an update to `docs-site/sales/table-ordering.md`; `README.md` pointed at this PRD.
**Done when:** a real guest completes a real QRIS payment on the real menu, the transaction appears on the POS badged "Order App" with the right table and the guest's name, and the `QRIS` wallet balance moves by the amount less MDR.

---

## Future Work (post-PRD)

- Notifying kitchen/bar when an `order` transaction is created, and notifying the guest when it is ready — the two explicit non-goals, and the natural next PRD now that a transaction exists to hang them on.
- More payment methods (virtual account, e-wallet) through the same `PaymentGatewayRepository` port and the `payments.method` column.
- Refunds and cancellation, which need a POS-side flow before they need a gateway call.
- Moving the POS's own QRIS acceptance onto DOKU so both channels reconcile from one source.
- A settlement wallet and an automated T+1 transfer (open question 4).
- Rate limiting the public and session routes (still open from D12 of `prd-table-ordering.md`, and now guarding a money path).
